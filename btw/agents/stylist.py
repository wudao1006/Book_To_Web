from btw.agents._placeholder import PlaceholderAgent


class StylistAgent(PlaceholderAgent):
    name = "stylist"
    description = "Defines cross-book visual and interaction rules."
    capability = "style"
