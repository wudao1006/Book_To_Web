from btw.agents._placeholder import PlaceholderAgent


class RetrieverAgent(PlaceholderAgent):
    name = "retriever"
    description = "Context retrieval service for book content."
    capability = "retrieval"
